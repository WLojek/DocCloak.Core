/**
 * @doccloak/core/dom - DOM-dependent submodule.
 *
 * Placeholder. Code that requires DOM APIs (DOMParser, canvas) such as docx
 * parsing and OCR is quarantined behind this export path so importing the
 * main entry never pulls in DOM references.
 */

export {};
