/**
 * @doccloak/core - public API surface.
 *
 * Placeholder entry point. Engine modules (types, session, pipeline, engine,
 * env, providers, regex, doc) move here from the DocCloak web app in later
 * extraction tasks. Until then this package exports nothing.
 */

export const CORE_PACKAGE_NAME = '@doccloak/core';
