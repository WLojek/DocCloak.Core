/**
 * @doccloak/core/worker-protocol - transport-agnostic worker message protocol.
 *
 * Placeholder. The message protocol plus proxy/host helpers (Web Worker
 * postMessage, chrome.runtime.Port, and future transports) move here during
 * extraction.
 */

export {};
